# 更新记录

## 2026-09-16 — v2.5 弹性报告期与第三方快照门禁

- 新增 `period`（阶段报告）：可使用任意连续 2 个月或以上的范围；月报、季度报告、年度报告仍严格限制为 1、3、12 个月。前序等长档案不完整时，报告会如实标记对比不可用，不再阻断当前完整报告或要求等长对比期。
- DataForSEO 与 SEOAgent 改为正常报告的默认必备模块。只有同客户、精确相同 `reporting_period`、带已批准标记且范围有效的本地快照才会复用；否则在读取凭据或付费调用前以 `THIRD_PARTY_APPROVAL_REQUIRED` 停止。用户明确放弃时必须传入 `--without-third-party`，并留下非敏感记录、隐藏对应面板。
- 发布器与 ZIP 一致性校验已支持 `period` 和共享周期规则。未采集、未改写客户归档、未调用第三方或发布任何报告。

## 2026-09-15 — v2.4 GA4 渠道会话近似计数容差

- 报告期 GA4 总会话与 `sessionDefaultChannelGroup` 渠道合计不再要求完全相等。差异不超过总会话的 2% 时，保留两边的官方原值并生成内部 `GA4_CHANNEL_SESSION_TOTAL_MISMATCH` warning；warning 记录总量、渠道合计、差额、比例和阈值，且不进入公开报告三件套。
- 差异超过 2% 时仍阻断生成；总会话为 0 而渠道合计非 0 也仍会阻断。`keyEvents` 保持原有严格相等校验，未被本次会话近似计数规则放宽。
- 新增回归：13,602 总会话与 13,654 渠道合计（0.38%）可生成并留下 warning；3% 差异仍阻断；关键事件差异仍阻断。未改写任何原始归档，未调用外部服务。

## 2026-09-10 — v2.3 共享 Google 原始月档案（计划中，尚未实现）

- 后续任务将实现共享 Google 原始档案根目录：GA4/GSC 月度源数据将按真实域名和月份保存于 `<archive-root>/ga4-gsc/<domain>/<YYYY-MM>.json`，与公开报告目录分离。
- 后续任务将实现普通报告的严格上期门禁：当前月或用于对比的上期源档案缺失时，将停止生成与发布；仅当期报告将只能在明确批准后例外执行。
- 后续任务将实现非公开来源摘要：每份报告将记录所用原始档案的相对路径与 SHA-256，发布前将复核该来源仍存在且未变化；该摘要不进入客户报告三件套。

## 2026-09-02 — v2.2.1 运营总结关键词口径修复

- 季报和年报的运营总结第 2 条现在只从 GSC 查询数据中选择非品牌词：先合并整个报告期，同一关键词按曝光加权计算平均排名，排除客户域名主词后按排名数值升序取前两条；月报仍使用当月数据。
- 提供已安装 v2.2 同事的安全同步方法：只替换全局 Skill 目录，旧目录移动到时间戳备份；不修改客户工作区的 `private/`、Google/第三方凭据、原始归档、报告输出或 `~/.codex/config.toml`，无需重新配置。
- 新增季度关键词回归测试；本次未采集、未改写客户归档、未重生成客户报告、未发布线上报告。

## 2026-08-29 — 正式打包与独立采集器依赖

- 正式包补入 `scripts/collection.py`，使已安装 Skill 的 `google_api_collector.py` 不再依赖工作区 `src/` 才能启动。
- 已重新生成 `seo-report-portal-v2-2.zip` 并安装到本机全局目录；Git 远端尚未推送，等待确认 v2.2 的仓库目标。

## 2026-08-29 — 运营总结国家/地区改用 GA4 自然搜索点击

- 运营总结第 6 条改为按 GA4 `organicGoogleSearchClicks`（Google 搜索自然点击次数）选择国家/地区；月报使用当月，季报与年报使用所选报告期的点击合计。
- 月度 GA4 归档新增 `ga4_organic_search_countries`，只含国家与该点击指标；既有归档不改写。GA4 未关联 Search Console 而缺少该指标时，报告如实显示“暂无可用数据”，不回退为会话、GSC 点击或展示。

## 2026-08-28 — 页面显示与运营总结统一 GSC 点击口径

- 所有客户可见的根路径 `/` 统一显示为“首页”；该映射只在展示层生效，GA4/GSC 原始路径不改写。
- 运营总结第 4 条页面排行改为按 GSC 页面点击量降序：月报使用当月，季报与年报使用所选报告期的点击合计。
- 看板“搜索表现 × 页面体验”页面表按同一 GSC 点击口径排序；季报/年报默认显示报告期合计，月报维持按月查看。原始路径仍保留在页面分析联动标识中。

## 2026-08-27 — v2.2 客户文字总结铁律

- 最终客户交付中的“文字总结”固定为已审核 `summary.md` 的 `## 运营总结` 标题及编号正文的逐字复制。
- 该文字总结不得因用户偏好或任何请求被改写、删减、重排、补充或替换；在线报告链接可单独提供。

## 2026-08-27 — 重点市场颜色条按点击量

- 重点市场仍按点击量降序；颜色条的宽度改为该市场点击量相对最大点击量的比例，不再使用展示量。
- “展示”与“点击”数值列保持不变，首行说明改为点击量最多的市场。

## 2026-08-28 — 运营总结首页名称

- `summary.md` 的运营总结将根路径 `/` 显示为“首页”；原始路径仍用于 GA4/GSC 数据合并、排序和诊断。
- “其次是 <路径>”继续表示报告期 GA4 会话量第二高的落地页，不是搜索表现 × 页面体验的排名。

## 2026-08-25 — 用户指定周期优先

- 明确报告周期以用户指定的连续月份范围为准；最后一个月尚未结束时，仍按该范围生成报告。
- 不再允许 Agent 擅自改为最近完整自然月，或自行加入“预览版”标签。
- 报告只汇总所选月度归档中实际已采集的数据；归档缺失、权限不足或采集失败仍会如实显示并按原有规则处理。
- 已将行动计划的“完整报告期”措辞改为“指定报告期”，避免暗示自定义期间一定是完整自然月。
- 发布门槛不变：仍须完成本地复核，并取得该报告的明确发布批准。
